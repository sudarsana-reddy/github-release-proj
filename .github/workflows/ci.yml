name: CI

on:
    push:     
    pull_request:      

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Code Checkout
              uses: actions/checkout@v3
            
            - name: build
              run: |
                mvn clean install